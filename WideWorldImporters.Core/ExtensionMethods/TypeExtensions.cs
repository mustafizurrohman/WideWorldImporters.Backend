using System;
using System.Collections.Generic;
using System.Text;

namespace WideWorldImporters.Core.ExtensionMethods
{

    /// <summary>
    /// Extension methods for Type
    /// </summary>
    public static class TypeExtensions
    {
        // keyObject.GetType().GetProperties().Select(p => t.GetProperty(p.Name)).ToArray();

        /// <summary>
        /// Gets the property.
        /// </summary>
        /// <returns></returns>
        //public static IEnumerable<string> GetProperty(this Object obj)
        //{
        //    var properties = obj.GetType().GetProperties()

        //    properties.
        //}
    }
}
